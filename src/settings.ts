export interface Settings {
    alwaysDisplayPinyin: boolean;
    useNotoSansSCFont: boolean;
}

export const defaultSettings: Settings = {
    alwaysDisplayPinyin: false,
    useNotoSansSCFont: false,
};
